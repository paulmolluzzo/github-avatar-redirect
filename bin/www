#!/usr/bin/env node
var debug = require('debug')('basic-api');
var app = require('../index');

app.set('port', process.env.PORT || 3000);

var server = app.listen(app.get('port'), function() {
  debug('Listening on port ' + server.address().port);
});
